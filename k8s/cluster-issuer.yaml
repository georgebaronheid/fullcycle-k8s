apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-k8s
  namespace: cert-manager
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: baronheid.george@gmail.com
    privateKeySecretRef:
      name: letsencrypt-k8s-secret
    solvers:
      - http01:
          ingress:
            class: nginx